name: Android Emulator CI with Video

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  emulator-video:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Android SDK
        uses: android-actions/setup-android@v3

      - name: Enable KVM group perms
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          ls -al /dev/kvm || true

      - name: Launch emulator and run script
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          profile: pixel_5
          cores: 4
          avd-name: test
          force-avd-creation: true
          emulator-boot-timeout: 1200
          disable-animations: true
          emulator-options: "-no-window -gpu swiftshader_indirect"
          script: |
            adb wait-for-device
            adb shell input keyevent 82
            sleep 10
            # سجل فيديو
            adb shell screenrecord /sdcard/demo.mp4 &
            # التقط صورة أولى
            adb exec-out screencap -p > screenshot1.png
            # افتح كروم على العنوان المطلوب
            adb shell am start -a android.intent.action.VIEW -d "https://rescend.netlify.app/" com.android.chrome
            sleep 20
            # التقط صورة بعد فتح الموقع
            adb exec-out screencap -p > screenshot2.png
            # مرر لأسفل الصفحة
            adb shell input swipe 500 1500 500 200
            sleep 5
            # التقط صورة بعد التمرير
            adb exec-out screencap -p > screenshot3.png
            # استخرج قائمة التطبيقات المثبتة
            adb shell pm list packages > apps.txt
            # أوقف تسجيل الفيديو
            adb shell pkill -l2 screenrecord
            # انسخ الفيديو من المحاكي
            adb pull /sdcard/demo.mp4 demo.mp4

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: emulator-artifacts
          path: |
            demo.mp4
            screenshot1.png
            screenshot2.png
            screenshot3.png
            apps.txt
